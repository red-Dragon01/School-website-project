import {student} from '@/data/data'; export default function Page(){return <main className="main"><p className="eyebrow">My Account</p><h1 className="title">Student Profile</h1><div className="card form" style={{marginTop:25}}>{[['Full Name',student.name],['Student ID',student.id],['Class',student.className],['Roll Number',student.roll],['Email',student.email],['Phone',student.phone],['Guardian',student.guardian]].map(([a,b])=><div key={a} style={{marginBottom:18}}><div className="muted">{a}</div><b>{b}</b></div>)}</div></main>}
