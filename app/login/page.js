'use client'; import {useState} from 'react'; import {useRouter} from 'next/navigation'; import Link from 'next/link';
export default function Login(){const [role,setRole]=useState('student');const [email,setEmail]=useState('student@demo.com');const router=useRouter();function go(e){e.preventDefault();router.push(role==='student'?'/student/dashboard':'/admin/dashboard')}return <div className="login"><div className="loginbox"><div className="eyebrow">BrightFuture Academy</div><h1>Portal Login</h1><p className="muted">Use this demo login to present the school management system.</p><form onSubmit={go}><label>Login as</label><select className="input" value={role} onChange={e=>setRole(e.target.value)}><option value="student">Student / Parent</option><option value="admin">School Admin</option></select><label>Email</label><input className="input" value={email} onChange={e=>setEmail(e.target.value)}/><label>Password</label><input className="input" type="password" value="demo1234" readOnly/><button className="btn" style={{width:'100%'}}>Login →</button></form><p style={{marginTop:20}}><Link href="/">← Back to website</Link></p></div></div>}
